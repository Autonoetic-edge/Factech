# Local sync of the Amfatec VPS trial

The matching Amfatec source is `E:\Factech`, not the older `E:\Facetech`
workspace. Seven application files were copied from the tested VPS trial and
their SHA256 hashes were checked against the running engine/gateway:

- `apps/verify/main.js`, `flow.js`, `framing.js`, `view.js`
- `engine/app/head_sequence.py`, `engine/app/challenge.py`
- `packages/face-auth/src/facetech_auth/operations.py`

Behavior: preparation waits for a steady face and restarts locally on sustained
early movement before submission. Settle range is 0.20, frontality 0.30, and the
actual Amfatec challenge issuer uses turn target 0.18. One leading opposite
sample in the second-turn window is allowed; two consecutive correct samples
per turn remain mandatory, and later reversals still fail. PAD, continuity and
identity checks are unchanged. The selected default route retains its historical
name; decision metadata identifies `amfatec-switch-trial-20260922`.

Backups and the seven-file hash manifest are outside the source tree at
`E:\facetech-vps-snapshots\local-sync-20260922`. No server deployment, credential
copy, Git initialization, commit or push was performed during this local sync.

Validation: SDK 214 tests; page 219 tests including seven preparation tests;
TypeScript clean; encoder/challenge/liveness/switch tests 101 passed across runs;
contract 14; mock gateway 149; head-gate replay suites 63. Existing tests were
updated for the new preparation event, target and authorized movement outcomes.
Original recorded fixtures remain unchanged; no new biometric data was copied.

Real-camera performance and spoof acceptance rates remain **unverified**.
This sync reproduces the VPS trial; it does not establish production accuracy.
