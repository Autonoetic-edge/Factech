# M0 — architecture and acceptance contract

Status: **COMPLETE (local design)**. Date: 2026-09-19. One agent. No deployment.

## Findings and decisions

The requested `E:\Factech\HARDENING\_PLAN.md` does not exist. The authoritative
file is `E:\Factech\HARDENING_PLAN.md`, confirmed by the README document map.
Read that plan, README and `pad-evidence/consent-v6/REVIEW.md`. No applicable
AGENTS.md was found in E:\ or E:\Factech and the edited subtrees. E:\Factech is
not a Git repository; `git rev-parse --show-toplevel` failed as expected. The
original E:\Facetech has pre-existing changes and was only inspected read-only.

Source inspection confirms shared principal access, caller-selected identities,
process-local templates/nonces, oldest-entry eviction, raw SQLite capture storage,
client metadata consent, unconstrained gateway body buffering, SDK transport versus
acceptance ambiguity and direct access to capture-store internals. Review findings
and additional plan concerns map to F01-F14 in ACCEPTANCE.md.

User explicitly selected **self-service only**. No assigned operator, reviewer or
administrator may enroll/add/verify/revoke on behalf of another participant. Review
uses separately granted tenant/round metadata and frame scopes. Architecture chooses
standard OIDC with Keycloak, PostgreSQL 17, separate evaluation/application storage,
versioned envelope encryption through a file-backed key provider, raw-frame backup
exclusion and a v2 decision API independent of FaceScan v1. These are implementation
contracts; providers/keys/databases were not provisioned.

All nine M0 checklist items are addressed in ARCHITECTURE.md and ACCEPTANCE.md.
Business inputs remain explicit: identity administrator/roster, retention purpose
and owner, key custodian, target load/hardware, SLO/RTO/RPO agreement, asset permissions
and independent review. They gate their later staging/production checks, not M0's
concrete design or synthetic implementation. No production readiness score assigned.

## Files changed

- Added `docs/hardening/ARCHITECTURE.md`, `ACCEPTANCE.md`, `routes.json`.
- Updated `HARDENING_PLAN.md`: M0 COMPLETE, its nine checks complete, M1/M6 IN_PROGRESS,
  evidence links and exact next action. Other milestone checkboxes remain unchanged.
- Updated README document map with architecture/acceptance/evidence links.
- Added read-only `tools/hardening_inventory.py`, source-inventory tests under
  `tests-hardening/`, pure M1 package under `packages/face-auth/`; see M1/M6 evidence.
- Saved original plan/README in `evidence/baseline/*.original`, 100 existing
  source/model/asset hashes in `baseline/source-sha256.json`, and observed check
  outputs in `local-checks.json`. No biometric fixture, database or secret copied.

## Commands and results

All implementation/check commands ran in PowerShell with working directory
`E:\Factech`. Runtime: `.venv-pad\Scripts\python.exe`, Python 3.12.14,
pytest 9.1.1, Ruff 0.16.8. Existing CI pins Ruff 0.14.14; CI was not executed.
No dependency installation, application import/startup, model inference or network
service request was needed. Official documentation was read for design selections;
source links are in ARCHITECTURE.md.

Discovery: `Get-Content -LiteralPath 'E:\Factech\HARDENING\_PLAN.md'` failed;
`rg --files` found `HARDENING_PLAN.md`. `rg --files --hidden -g AGENTS.md
-g '!vendor/**' -g '!.venv-pad/**' -g '!.git/**'` found no repository instructions;
the E:\ parent was also checked. Source reads used explicit file paths and `rg`.
No `.env`, password, token, private key or capture payload was read or printed.

Executed focused validation:

```powershell
.\.venv-pad\Scripts\python.exe -m pytest tests-hardening -q -p no:cacheprovider
.\.venv-pad\Scripts\python.exe -m ruff check packages/face-auth/src tests-hardening tools/hardening_inventory.py
.\.venv-pad\Scripts\python.exe -m ruff format --check packages/face-auth/src tests-hardening tools/hardening_inventory.py
.\.venv-pad\Scripts\python.exe tools/hardening_inventory.py
```

Final results: **186 passed in 0.51s**; Ruff passed; six Python files already
formatted; **19 explicit method/path registrations** have policies and acceptance
references. Static/framework route enforcement is explicitly not proven by AST checks.

Initial run: 180 tests passed, but Ruff reported I001 import ordering in the new
authorization test. Fixed only the new files with `ruff check --fix`, formatted,
then added six malformed-timestamp denial cases and reran the affected checks.
No existing suite was rerun unchanged. No failed check is hidden by the final pass.

Final documentation validation initially assumed ten M0 checklist bullets and failed.
Comparison with the preserved original confirmed nine; corrected the evidence wording
and the checker to derive its expected count from the original plan. All nine are
complete; no additional milestone checklist items were marked complete.

The following read-only Python check was executed through a PowerShell single-quoted
here-string piped to `.venv-pad\Scripts\python.exe -`:

```python
import hashlib, json, re
from pathlib import Path
root = Path.cwd()
manifest = json.loads((root / 'docs/hardening/evidence/baseline/source-sha256.json').read_text('utf-8'))
changed = []
for name, expected in manifest.items():
    with (root / name).open('rb') as stream:
        if hashlib.file_digest(stream, 'sha256').hexdigest() != expected:
            changed.append(name)
print(f'Baseline comparison: {len(manifest)} files, {len(changed)} changed.')
if changed:
    raise SystemExit('\n'.join(changed))
architecture = (root / 'docs/hardening/ARCHITECTURE.md').read_text('utf-8')
acceptance = (root / 'docs/hardening/ACCEPTANCE.md').read_text('utf-8')
assert '**self-service only**' in architecture
ids = set(re.findall(r'^\| ([A-Z][0-9]{2}) \|', acceptance, re.MULTILINE))
routes = json.loads((root / 'docs/hardening/routes.json').read_text('utf-8'))
references = {a for route in routes['explicit_routes'] for a in route['acceptance']}
assert references <= ids, references - ids
print(f'Acceptance mapping: {len(ids)} criteria/findings; all {len(references)} referenced route criteria exist.')
```

Observed: 100 baseline files, **zero changed**; 58 acceptance criteria and all 15
route-referenced criterion IDs present. F01-F14 finding rows are additional to the
58 criteria. Snapshot covers the existing source, models, browser assets and deploy
configuration; new policy/tests/docs are intentionally outside that old-file set.

## Limitations, rollback and handoff

M0 completion means design and acceptance contract delivered. M1 is a locally tested
pure policy foundation, not operational authentication. M2-M5/M7/M8 are not completed.
M6 has a source guard and traceability only. No actual OIDC round trip, database,
encryption/restore, HTTP authorization, load, real device or hosted CI was tested.

Rollback: no running release needs rollback. To undo this local work, first review
later edits, restore only plan/README from `baseline/*.original`, and remove only
the new hardening docs/package/tests/tool if still unmodified by later work. Do not
run historic deploy/rollback scripts or reset E:\Facetech. Since there is no Git
in E:\Factech, the saved originals are the document recovery source.

Live changes: **none**. No SSH, VPS requests, service stop/start/recreate, migration,
capture read/download/delete/relabel, export enabling or port-8000 interaction.
This is a statement of actions taken, not a fresh independent inventory of live.

Weekly account usage: start 68%, after local changes/tests 68%, final checkpoint
68%: **+0 percentage points** (rounded account-wide reading). No deployment checkpoint
was applicable because no deployment was attempted.

Next: implement the OIDC/session adapter and durable identity mappings, then wire the
shared policy into explicit hardened routes at gateway and engine with audit and
fresh-state commit checks. Follow M1 evidence for missing gates. M2 synthetic schema
work may proceed against the completed identity contracts. Preserve live freeze.
