# M6 — QA foundation

Status: **IN_PROGRESS**, not the integrated QA gate. Updated: 2026-09-19.

## M3 completion — consent, encryption, retention and recovery

The final integrated run passed **666 tests in 233.77s**, zero failures/errors/skips,
with one unsuppressed Authlib HTTPX deprecation warning. The 617 M2 cases remain;
49 M3 cases cover server consent, optional recording, withdrawal races, envelope
context/key tampering, key loss/rotation, expiry, linked purge, injected disk and
commit/acknowledgement failures, bounded retries, and encrypted allowlisted recovery.
Lint/format passed for 41 Python files; all 100 protected hashes remain unchanged.
Exact evidence: `m3-completion-tests.xml`, `m3-completion-checks.json`,
`m3-completion-source-sha256.json`; failed runs and fixes remain in M3.md.

P01–P08 close the isolated M3 boundary. One new real-Keycloak/two-HTTPS-process case
records a frozen-parser rejection under optional consent, verifies the status-only
receipt, withdraws consent and purges the encrypted record. This distinguishes
recording from biometric acceptance. Purpose-restricted providers and separate
service fixture files are tested; OS service/key permissions remain M7.

The recovery test uses an encrypted application-only logical snapshot, a newer
withdrawal ledger with independent digest/freshness checkpoint, original expiry,
and a separate owned restore DB. Missing/stale/tampered ledgers and missing keys
keep durable quarantine; another repository also denies reads. This does not
complete M7's independently durable ledger publication, backup schedule/custody,
key recovery packaging, delivered alerts or measured clean-host RTO/RPO.

Q02/Q03 now have consent/privacy/key/disk/purge failure evidence. Q01 hosted CI,
M4 saturation and resource bounds, M5 SDK combinations, and Q04–Q06 device/physical
cohorts remain open. The guided preview is unchanged, simulated and unintegrated;
its browser URL restriction was not bypassed. No scores or efficacy claims are
upgraded by synthetic results. M6 remains IN_PROGRESS; M4 is the next milestone.
Owned processes are stopped, four test ports are free, and live/original-checkout
changes are none. Weekly account-wide usage: **10% → 17%, +7 percentage points**.

## M2 completion — frozen pipeline persistence, idempotency and recovery

The final integrated run passed **617 tests in 199.23s**, zero failures, errors or
skips, one unsuppressed Authlib HTTPX deprecation warning: the 588 M1 tests plus
21 durable-state tests (`test_durable_state.py`) and eight idempotency-key edge
tests. Lint and formatting passed for 32 Python files; all 100 protected
source/model/asset hashes remain unchanged. Exact results and source identity are
in `m2-completion-tests.xml`, `m2-completion-checks.json` and
`m2-completion-source-sha256.json`; failures observed during development and
their fixes are in M2.md.

New coverage: the unchanged engine pipeline behind the Analyzer port with mocked
model outputs (as `engine/tests/test_secure_pad.wired_client`) and with the real
ONNX models on synthetic pixels; legacy-versus-hardened decision parity; restart
and separate-process reads; four-process nonce races; capacity boundaries under
concurrency; revocation during analysis; idempotent replay, conflict, in-progress
and lost-response cases; store outage, fingerprint mismatch, tampered ciphertext
and commit abort; a genuine 0001→0002 forward migration with old-application
refusal; and a `pg_dump`/`pg_restore` quarantine drill applying the deletion
ledger. The real-Keycloak two-process staging engine now runs the frozen analyzer.

D01–D08 close the M2 boundary. Q01 remains partial without hosted CI/exact image
builds. Q02/Q03 now have inference wiring, datastore failure, migration and
recovery evidence but still lack consent/privacy, key, disk and purge failures,
saturation and supported SDK combinations. Q04–Q06 remain open for actual devices
and cohorts. No physical efficacy, live rollout, production SLA or readiness
score is claimed. The temporary stack is stopped; no live/original-checkout
change occurred. Weekly usage could not be read from this session (unverified).

## M1 completion — durable authorization and real provider staging

The final explicit integration run passed **588 tests in 115.99s**, zero failures,
errors or skips, and one unsuppressed Authlib HTTPX deprecation warning. This
includes the original 186 tests, the earlier 358 OIDC/HTTP tests and 44 new
PostgreSQL/Keycloak cases. Lint and formatting passed for 28 Python files; all
100 protected source/model/asset hashes remain unchanged. Exact results and source
identity are in `m1-completion-tests.xml`, `m1-completion-checks.json` and
`m1-completion-source-sha256.json`. See M1.md for observed failures and their fixes.

New `postgres_fixtures.py`, `test_postgres_authorization.py`, and
`test_real_keycloak.py` add real transactions, separate repository connections,
20-consumer login/nonce races, audit-commit rollback, authority changes during
analysis, grant changes during evaluation reads, SQL pagination, durable logout
retry, and real Keycloak authorization-code/PKCE/TLS login/logout. Gateway and
engine run as distinct loopback HTTPS processes for the provider tests. Provider
tokens are confirmed inactive after logout. JSON/MessagePack self workflows and
no participant delegation are checked with synthetic payloads only.

A01-A09 close the M1 boundary; the current mapping and identity transition are in
M1.md and M1_RUNBOOK.md. Q01 remains partial without hosted CI/exact image builds.
Q02/Q03 now have real database and provider evidence but still lack the complete
inference/privacy/idempotency pipeline, disk/purge/key/recovery failures, saturation
and supported SDK combinations. Q04-Q06 remain open for actual devices/cohorts.
No physical efficacy, live rollout, production SLA or readiness score is claimed.

The temporary synthetic stack is stopped. No live/original-checkout changes,
capture access, model or threshold change occurred. This continuation began at 0%
weekly usage after a reset, reached 3% during checks and 4% after the full suite;
the final checkpoint/delta is in the completion summary. Next: M2's frozen
inference boundary, durable idempotent operations and recovery, then later gates.

## Earlier M1 continuation — historical isolated authentication and HTTP checks

The focused hardening suite now has **544 tests passing** (186 original plus 358
new), with lint passing and 15 Python files formatted. Final full run: 28.83s,
one unsuppressed Authlib HTTPX deprecation warning. A later documented-registry
assertion was verified by rerunning that one changed test (0.55s); runtime code
did not change after the full run. `m1-local-tests.xml` records the full run.
Exact commands, dependency versions, failures/fixes and limitations are in M1.md.

New `test_oidc_sessions.py`, `test_http_authorization.py`, `test_http_edges.py` and
`auth_fixtures.py` exercise the real Authlib/joserfc/crypto adapters through mocked
HTTPS and in-process ASGI requests. These check login/session/CSRF, both transports,
both service boundaries, no participant delegation, review scopes, every protected
registration's authentication, route/method bypasses, provider/database/audit outage,
logout retry and revocation during synthetic work. There are no sockets or captures.
Synthetic operation receipts do not assert enrollment or biometric acceptance.

`hardened-routes.json` separately documents 24 gateway and 11 engine registrations;
the registry test detects undocumented drift. The original AST inventory still
maps 19 legacy registrations. All **100 baseline source/model/asset hashes remain
unchanged**. Added source checksums and machine-readable check summaries are in
`m1-source-sha256.json` and `m1-local-checks.json`. No Git revision is available.

Observed failures are retained in M1 evidence: the initial six lint findings;
then four failing edge tests revealing a missing scan-field exception (two service
cases), malformed-provider error classification and logout audit attribution.
All were fixed and affected checks rerun. Do not replace these facts with a claim
that every development run passed.

Q01 remains partial: local traceability is present, hosted CI and reproducible image
builds are not. Q02/Q03 are not satisfied by an in-memory repository or synthetic
operation adapter; actual transactions, list pagination, replica consistency,
database/disk/key/purge/model failures and integrated workflows remain. Q04-Q06
still require team devices and independent physical cohorts. M1's fresh-state
callback test is not proof of atomic commit behavior. No readiness score assigned.

Usage this continuation: 69% -> 70% -> 71%, **+2 percentage points** at checkpoints.
Live/original checkout changes: none. Next: durable M1 repositories plus M2/M3
transactional adapters, followed by relevant isolated integration/failure tests.

## M0 foundation (historical)

Added acceptance IDs B01-B05, A01-A09, D01-D08, P01-P08, S01-S07, C01-C06,
Q01-Q06, O01-O06 and R01-R03, with review finding mapping F01-F14.
`docs/hardening/routes.json` classifies 19 existing explicit method/path registrations,
plus separately recorded framework routes, static mounts and proposed future routes.

`tools/hardening_inventory.py` uses Python AST and JSON only. It reads source without
importing FastAPI apps or opening capture storage; it fails when an explicit route
changes without a corresponding policy/acceptance entry. Three source-inventory
tests include unreviewed-route and missing-policy mutations on temporary source
copies. The authorization tests exercise synthetic actors/tenants/grants only.

Commands/environment/results and one fixed lint failure are recorded in M0.md and
local-checks.json: **186 tests passed**, focused lint and formatting passed. This
provides local traceability and regression checks, not runtime route enforcement,
hosted CI, OIDC, concurrency/database recovery or biometric efficacy evidence.

No workflow, capture UI, SDK capture timing, model, threshold or live test evidence
changed. No existing suite was rerun without a relevant change. New checks are not
yet wired into CI; that integration must build the actual hardened package/artifacts.

Remaining: full Q01-Q06 and dependent milestone gates, failure injection, integrated
two-tenant E2E, replica races, measured isolated load and recovery, real Samsung A13
checklist execution, independent physical cohorts and release-linked CI outputs.
Physical and engineering evidence must remain separate by model/policy/round.

Rollback: remove the new source-only tool/tests only after checking subsequent edits;
there is no deployed artifact to roll back. Next: expand traceability as M1 adapters
and route guards are implemented; replace no real checks with these source guards.
Usage: 68% -> 68%, +0 percentage points. Live changes: none.
