# Exact pins: a rebuild must produce the same runtime, not "whatever resolves today".
# Versions are the ones this workspace runs and tests against (2026-09-20).
# Transitive dependencies are still resolved by pip; a hash-locked file is O01 work.
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
msgpack==1.2.2
numpy==2.5.3
opencv-python-headless==4.11.0.86
onnxruntime==1.30.0
